# Legacy OpenPI Takeover Experiments

These experiment outputs were moved out of the active OpenPI experiment root on
2026-06-26.

Reason: they evaluate OpenPI as a saved dynamic takeover policy, often from
DP/Cosmos-derived snapshots and with DP96 handoff labels. That protocol is not
the active method.

They are preserved as diagnostic history only. Do not cite them as OpenPI
method success. See `MOVED_DIRS.txt` for the moved paths.

The active OpenPI status is:

`experiments/world_model_task_rebinding/openpi/OPENPI_FULL_EPISODE_ACTIVE_STATUS_20260626.md`
