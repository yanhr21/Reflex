# Legacy OpenPI Setup Attempts

These directories were moved out of the active OpenPI experiment root on
2026-06-26 because they are failed or intermediate setup/conversion/training
attempts.

Kept active:

- final 733 LeRobot conversion root;
- fixed 733 LeRobot audit root;
- full-episode OpenPI training roots with `training_walltime_summary.json`;
- preserved checkpoints;
- LeRobot data home and OpenPI data home.

See `MOVED_DIRS.txt` for the moved paths.
